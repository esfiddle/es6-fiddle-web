// Example for ECMAScripts 2016+ version

// A simple replacement for Math.pow function is the exponentiation operator(**).

// without exponentiation operator
console.log(Math.pow(4, 3));   // ANS = 4*4*4 = 64

// with exponentiation operator
console.log(4 ** 3);   // ANS = 64
